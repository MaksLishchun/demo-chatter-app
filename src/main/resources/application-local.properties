# Налаштування бази даних
spring.datasource.url=jdbc:postgresql://localhost:5432/chatter_db
spring.datasource.username=ml
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# Налаштування JPA/Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Налаштування серверу
# server.port=8081

# flyway
spring.flyway.url=jdbc:postgresql://localhost:5432/chatter_db
spring.flyway.user=ml
spring.flyway.password=password

server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=35152342
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=springboot